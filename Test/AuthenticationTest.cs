using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Test
{
    [TestClass]
    public class AuthenticationTest
    {
        [TestMethod]
        public void SecretToken()
        {
           // Flickstein.Authentication.OAuth oAuth = new Flickstein.Authentication.OAuth("1ce7081938204ed1cab66edebf67e76b", "415488cb491e4d6f", "http://localhost:23019/Example-Callback.aspx");
           // String secretToken = oAuth.GetSecretToken();
            
           // Assert.AreNotEqual(String.IsNullOrEmpty(secretToken), true);
        }
    }
}
